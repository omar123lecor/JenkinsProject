name: "sales_dbt_project"
version: "1.0.0"
config-version: 2

profile: "default"

model-paths: ["models"]
target-path: "target"
clean-targets: ["target"]

models:
  sales_dbt_project:
    +materialized: view
